# Public backend API URL (no trailing slash).
# Local:
# VITE_API_URL=http://localhost:5000
# Production (your hosted backend, e.g. Railway / Render / Fly):
VITE_API_URL=https://your-backend-url.example.com
